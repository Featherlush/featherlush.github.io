const n='---\ntitle: "SFML 课设里 Game 与 Tetris 怎样拆分"\nexcerpt: "游戏课设里 SFML 窗口、双人对战与单盘面怎样分层，main 到 gameRun 一整帧里发生了什么。"\ncategory: "游戏开发"\ncategoryId: "game"\ndate: "2025-09-18"\nauthor: "徐宁"\nproject: tetris-ai\nseries: tetris-notes\n---\n\n游戏开发课设 **Tetris AI** 不是从零写引擎，而是在课程 **CMake 与 SFML** 模板上加 AI。仓库在本地课设目录，不在 Web 栈里。读代码时我按 **谁管窗口、谁管盘面** 来拆，入口极薄，`Game` 管平台，`Tetris` 管规则。这篇只谈架构与一帧流程，AI 评分见 [另一篇](/blog/tetris-ai-heuristic-search)。\n\n## 一、CMake 与目标划分\n\n`CMakeLists.txt` 做三件事。\n\n1. `FetchContent` 拉 **SFML 3.0.1** 源码并编译  \n2. `add_library(GameLib … Game.cpp Tetris.cpp PD.cpp)` 把游戏逻辑打成静态库  \n3. `add_executable(main src/main.cpp)` 链接 `GameLib`\n\n`POST_BUILD` 把 `data/` 拷到 `build/bin/data`，避免运行时找不到贴图。MinGW 下额外 `-static-libgcc -static-libstdc++`，减少 DLL 依赖。\n\n这种拆分的好处是 **改 AI 只动 `PD.cpp`，改绘制坐标只动 `Game.cpp` 或 `Tetris::Draw`**，链接入口不变。\n\n## 二、类职责对照\n\n| 类 | 定位 | 典型成员 |\n|----|------|----------|\n| `Game` | 操作系统与 SFML 的壳 | `RenderWindow window`、`player1` 与 `player2`、`gameInput` 与 `gameDraw` |\n| `Tetris` | 单侧玩家的完整状态机 | `Field[][]`、当前块坐标、`score`、`AI` 标志 |\n\n`Game.h` 里写得很直白，`Tetris` 是一个小游戏，`Game` 负责 **衔接 Tetris 与操作系统**。因此纹理 `tBackground`、`tButtons` 在 `Game` 加载，方块条 `tTiles` 传给两个 `Tetris::Initial`；按键在 `Tetris::Input` 里按 `rolePLAYER1` 与 `rolePLAYER2` 分支；胜负贴图 `DrawResults` 在 `Game` 统一画在两侧盘面顶部。\n\n`Tetris` 不创建窗口，只持有 `RenderWindow*` 用于 `Draw()` 里 `window->draw`。\n\n## 三、主循环与对局状态\n\n`main` 只有几行。\n\n```cpp\nGame tetrisGame;\nwhile (tetrisGame.window.isOpen())\n    tetrisGame.gameRun();\n```\n\n`gameRun()` 用 `do { … } while (!isGameQuit)` 包了两段循环。进行中的单帧骨架如下：\n\n```cpp\nvoid gameRun() {\n    gameInput();\n    if (isGameBegin && !isGameHold) gameLogic();\n    gameDraw();\n}\n```\n\n1. **进行中**，`isGameOver == false` 时，每帧依次 `gameInput`、在 `isGameBegin` 时 `gameLogic`、`gameDraw`  \n2. **已结束**，`isGameOver == true` 时，只处理 `gameOverInput` 与绘制，点结束可 `isGameOver = false` 重开  \n\n未点开始 时，`gameLogic` 会反复 `gameInitial()`，相当于停在准备界面。\n\n对局级布尔量都在 `Game`。\n\n- `isGameBegin` — 是否已开始，影响是否调用 `gameLogic`  \n- `isGameHold` — 暂停时 `gameLogic` 直接 return  \n- `isGameOver` — 任一侧 `Tetris::isGameOver` 触发  \n\n`gameInitial()` 里 noteworthy 的配置如下。\n\n```cpp\nwindow.setFramerateLimit(15);\nplayer2.AI = true;   // 默认 P2 托管\n```\n\n帧率 15 是课程模板的节奏感设定；AI 每帧 `logicAI()` 硬降一颗，视觉上 P2 会非常快。\n\n## 四、单帧数据流\n\n一帧大致经过这些步骤。\n\n1. `pollEvent`，Game 处理开始、暂停、换肤与关闭，两个 `Tetris::Input` 各自读键  \n2. `gameLogic`，未暂停且已开始时，时钟驱动 `player1.timer` 与 `player2.timer`，再调两侧 `Logic()`  \n3. `gameDraw`，画背景与 frame，两侧 `Draw()`，cover 遮罩，按钮与说明文字，最后 `DrawResults`  \n\n`Tetris::Logic()` 内部分叉如下。\n\n- `AI == false` 时走 `traditonLogic`，移动、旋转、重力、硬降，带消行动画分支  \n- `AI == true` 时走 `logicAI`，算落点、硬降、写 Field、生成新块  \n\n手动与 AI **共用** `Field`、`hitTest()`、`newShapeFunc()`，避免两套规则漂移。\n\n## 五、Tetris 侧，场地与坐标\n\n常量集中在 `Tetris.h`。\n\n- 逻辑场地 `FIELD_WIDTH × FIELD_HEIGHT`，10×24，可见舞台 `STAGE_HEIGHT` 20  \n- 行 0–3 为顶部缓冲，行 4 起进入可见区；`Draw` 里 `y > 3` 才画活动块  \n- `GRIDSIZE 35` 像素，盘面左上角 `mCornPoint` 由 P1 与 P2 不同宏定义  \n\n`Field[i][j]` 存颜色编号，0 空，1–7 七种块色。碰撞 `hitTest()` 检查越界与 `Field` 占用。\n\n现代玩法在 `Tetris.cpp`。\n\n| 机制 | 函数 | 要点 |\n|------|------|------|\n| Bag7 | `Bag7()` | 7 块一轮不重复 |\n| Hold | `holdFunc()` | 与当前块交换；hold 空时从 Next 取 |\n| 阴影 | `shadow()` | 模拟落到底，`shadowSquare` 供硬降 |\n| 底部缓冲 | `slowLoading()` | 触底前先试探下移，延长 `delay` |\n| Wall kick | `rotateFunc()` | 旋转失败则回退，修正重心高度差 |\n| 消行动画 | `checkLine` 与 `animationFunc` | 手动模式先播动画再 `clearLine` |\n\n`isWin()` 用第 4 行是否有块判负，比单纯碰撞出生点更简单，但堆太高时即结束。\n\n## 六、资源与绘制顺序\n\n`LoadMediaData()` 按 `imgSetNo` 加载 `bg1–4.jpg`、`tiles1–4.jpg`，以及 `frame.png`、`button.png`、`end.png` 等。按钮用同一张纹理的 **8 个横向切片**，`ButtonState` 枚举 × 宽度 110。\n\n`Tetris::Draw` 顺序值得记。\n\n1. 阴影，灰色 tint  \n2. **活动块**，在固定块之前，避免被遮挡  \n3. 固定块，消行动画行特殊处理  \n4. Next 与 Hold 预览  \n\n`Game::gameDraw` 在两侧 `Draw` 之后盖 `cover.png` 做盘面遮罩——美术框与逻辑格对齐靠宏坐标，改分辨率时要一起改。\n\n## 七、和 Web 前端的类比\n\n若习惯 Vue 项目，可以这样映射。\n\n- `Game` ≈ App 壳与全局状态，是否开始、暂停  \n- `Tetris` ≈ 一个游戏组件实例，props 是 `role`，内部 state 是 `Field`  \n- `gameRun` ≈ `requestAnimationFrame` 里固定调 input、update、render  \n\n课设里没有 ECS、没有场景树，**两个 `Tetris` 对象就是全部玩法状态**——简单，但 AI 与动画分支都挤在 `Logic()` 里，阅读时要靠 `AI` 与 `animationFlag` 开关理清路径。\n\n和同年早些时候的 **图形学软光栅课设** 对比，SFML 帮你管了帧缓冲与纹理上传，但 **input、update、draw** 的主循环结构是相通的。\n\n## 小结\n\n架构核心就一句，**Game 管窗口与双人编排，Tetris 管单侧盘面，PD.cpp 专管 AI 模拟与评分**。AI 如何在 `getPosition` 里穷举落点、`calculate` 的六个特征从哪来，见 [启发式搜索篇](/blog/tetris-ai-heuristic-search)。\n\n标签：工程化\n';export{n as default};
