const e="---\ntitle: 论文版七块视图的 Grid 与 Pinia 联动\nexcerpt: 电脉智绘论文版如何用 14×23 CSS Grid 拼七块视图面板、Pinia 多 Store 广播，以及 D3 容器化重绘为何是定制图表的前置条件。\ncategory: 前端开发\ncategoryId: frontend\ndate: 2025-04-08\nauthor: 徐宁\nproject: electricity-forecast-paper\nseries: d3-notes\n---\n\n2025 年 4 月做电脉智绘论文版时，第一个决策不是先画哪张 D3 图，而是 **仪表盘怎么摆**。Control Panel、时序、层级、相关矩阵、评估、异常，七块视图要同时出现在一屏里，还要和论文插图 a 至 g 对应。这篇讲 `App.vue` 的 **14×23 Grid**、**Pinia 状态总线** 和 **D3 容器契约**，后面所有外壳加异构微图能联动的前提都在这里。\n\n## 一、为什么是 Grid 与全屏，而不是 Tab\n\n论文读者要在同一屏里对比：选了哪个节点、时序怎么变、层级误差如何、特征矩阵是否相关、四维指标落在哪。Tab 切换会打断这种扫视式阅读。大创应用版用 Tab 适合操作流程；论文版用 **固定网格分区**，每格 hover 微阴影，像学术海报上的子图排列。\n\n`App.vue` 核心布局：\n\n```css\n.view-container {\n  display: grid;\n  grid-template-rows: repeat(23, 1fr);\n  grid-template-columns: repeat(14, 1fr);\n  gap: 2px;\n  width: 100vw;\n  height: 100vh;\n}\n```\n\n各面板占格如下。\n\n| 组件 | grid 区域 | 论文子图 |\n|------|-----------|----------|\n| `DataOverview` | 行 1：7，列 1：3 | (a) Control |\n| `FeaturePannel` | 行 7：15，列 1：3 | (b) Feature |\n| `TimeSeriesView` | 行 1：10，列 4：12 | (d) Time Series |\n| `FeatureMatrixView` | 行 15：24，列 1：3 | (c) Correlation |\n| `HierarchyView` | 行 10：24，列 4：12 | (e) Hierarchy |\n| `EvaluateView` | 行 1：9，列 12：15 | (f) Evaluation |\n| `AnomalyViewTrue` | 行 9：24，列 12：15 | (g) Anomaly |\n\n`1fr` 行高让各块随视口等比伸缩；子组件 `width/height: 100%` 加 `overflow: hidden`，D3 在 `onMounted` 与 `resize` 时读 `clientWidth`。\n\n### 版面带来的技术约束\n\nGrid 不是纯 CSS 装饰，它 **反向约束每张 D3 图的实现**。\n\n| 格子 | 约束 | 触发的定制决策 |\n|------|------|----------------|\n| 左列窄 | 相关矩阵 `svgHeight = clientWidth` 正方形 | 轴 8px 字、扇形格 `tickMargin` 手算 |\n| 中部宽 | 时序上下分层、tooltip 向左大偏移 | 双 SVG 外壳 |\n| 中部高 | 层级树逻辑尺寸溢出 | `scale(0.3)` 加 `translate(675,20)` |\n| 右列窄 | 评估条宽 `value/max` 居中 | 非满格 heatmap |\n\n**先定 Grid，再定每张图读哪个尺寸字段**，若先按固定画布开发，搬进 Grid 必全面返工。\n\n## 二、Pinia 谁驱动谁重绘\n\n论文版按 **交互维度拆 Store**，各 D3 组件 `watch` 自己关心的字段。\n\n| Store | 典型字段 | 触发的视图 |\n|-------|----------|------------|\n| `TreeNode_transfer` | `selectedNodeName`、`nodeResponseData` | 时序、矩阵、层级、异常 |\n| `Interval` | `startDate`、`endDate` | 时序、评估 |\n| `PredictionDuration` | `duration`7/10/30/90 天 | 时序底栏日历格数量 |\n| `SelectedTimeStore` | `selectedTime`点击某天 | 评估、异常拉取 |\n| `State` | `currentModelValue`、`featurestate` | 全局面板刷新 |\n| `TreeNode_SwitchAnomaly` | 异常高亮开关 | 时序预测区着色 |\n\n典型链路如下。\n\n1. 用户在 `Tree.vue` 点击叶子节点\n2. `setSelectedNodeName` 与 `getElectricityConsumption` 拉预测 JSON\n3. `TimeSeriesView` 的 `watch(nodeResponseData)` 触发 `initializeCharts()`\n4. 白名单叶子 到 `FeatureMatrixView` 请求 `/featureSelection`\n5. `SelectedTimeStore` 更新 到 `EvaluateView` 请求 `/getPeriodErrors`\n\n### 广播模式 vs 父子 emit\n\n**事件写 Store，多 watcher 各自重绘** 适合七块 **独立 SVG 画布**：没有共同父组件包一层 ChartProvider，也不希望 `App.vue` 知道 D3 内部细节。\n\n**障碍：字段命名不一致**，`selectedNodeName` 与 `selectedNodeeName` 并存，不同组件 watch 不同字段。联调时会出现树点了、矩阵不刷，属于 **Store 契约未文档化** 的坑，后续相关矩阵、时序篇都会再提到。\n\n**障碍：重复请求**，换模型时 `State` 与 `Interval` 可能连续触发多个 watch，评估区会连拉两次 API。论文 demo 用 `message.loading` 缓解卡住感，未做请求去重，取舍是 **简单可靠优先**。\n\n## 三、D3 与 Vue 的分工\n\n每个面板遵循同一模式：**模板**放标题、说明，`chartRendered` 门控和 `ref` 容器；**脚本**里 `axios` 拉数后 `drawChart` 与 `initializeCharts`，`watch` Store 触发清空重绘，`onMounted` 与 `resize` 更新尺寸。\n\n**不在 Vue 模板里写 SVG 结构**，D3 动态 append；Vue 只管容器与显隐。\n\n重绘前统一：\n\n```javascript\nd3.select(container).selectAll('*').remove()\n```\n\n避免切换节点时旧 path 残留。`FeatureMatrixView` 用 `.matrix` 类局部清空；`EvaluateView` 用 `#chart`，容器 id 要稳定，否则 resize 监听器绑错 DOM。\n\n工具函数抽到 `utils/`：\n\n- `drawPetalRing.ts`，花瓣环层级叶子\n- `drawTreeNode.ts`，中间层 Glyph\n\n组件保留 **数据绑定 与 交互**；纯几何进 utils，方便对齐论文 `scaleFactor`、`padAngle`。\n\n### chartRendered 门控\n\n矩阵、时序、评估都有 `chartRendered`：只有 SVG 成功绘制后才显示说明英文。空数据或 cutoff 日期外不显示文案，避免读者对着空白格读 caption。\n\n## 四、定制图表在仪表盘中的位置\n\n七块视图可归纳为 **三种外壳**，细节在系列后续各篇展开。\n\n| 外壳类型 | 对应视图 | 格内 或 区内图形 |\n|----------|----------|----------------|\n| band 矩阵 | Feature Correlation | 色块、直方图、散点 |\n| band 矩阵 | Evaluation View | 居中宽度条 |\n| tree 布局 | Hierarchy View | Glyph 与 Petal 与 三角连线 |\n| 上下分区 | Time Series View | 折线区与日历格区 |\n\n仪表盘本身不提供绘图 API，只提供 **格子的宽高比与联动事件**。每一格的外壳几何不同，格内图类型还可能按索引分支，没法用一个 ECharts option 包圆。\n\n## 五、与 ECharts 的边界\n\n`package.json` 虽有 ECharts，论文版 **核心定制图全是 D3**。ECharts 适合标准折线与柱状；这里需要矩阵格内嵌 histogram 与 scatter、非标准极坐标花瓣、历史与预测分界竖线与左右不同准星、三角渐变填充连线、转置日历格加双色系。用配置型库会花在 hack 坐标系上的时间多于手写 D3。\n\n## 六、开发节奏与并行策略\n\n4 月上旬锁定 Grid 与 Store 字段表，后端接口与论文子图编号对齐；再并行左列 Control 与 Feature 与 Matrix、中部时序与层级树、右侧评估与异常。\n\n**未等全部 API**，`Tree.vue` 用 `baseData` 静态树调布局，接口通了再 `watch` 换真数据。相关矩阵、时序也可用 mock JSON 先调 `drawChart` 管线。\n\n### 真实障碍 Grid 行号与 CSS 溢出\n\n`grid-template-rows: repeat(23, 1fr)` 与组件里行 15：24 等注释 **允许最后一行溢出到视口外** 或靠 `100vh` 裁剪，开发时若发现相关矩阵下半被切，要查 **父级 overflow** 与 **子组件是否假设额外行存在**。`gap: 2px` 是为海报子图缝；去掉 gap 会让窄栏评估条更易贴边。\n\n## 小结\n\n论文版前端的框架不是某个 UI 库，而是 **全屏 Grid 分区、Pinia 多 Store 广播与 D3 容器化重绘**。Grid 决定每张图的可用像素与宽高比；Store 决定何时清空重画；`draw*` 函数决定格内画什么。最难定制的 Feature Correlation 会在同系列后续篇单独展开，同一 SVG 里三种单元格类型如何共存，以及矩阵外壳加异构微图的可复用范式。\n\n标签：D3.js, Vue.js, CSS, 数据可视化\n";export{e as default};
