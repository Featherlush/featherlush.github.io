const n='---\ntitle: "双人方块 AI 的六维启发式落点搜索"\nexcerpt: "托管模式下每一步怎么选旋转与横移，特征提取、加权评分，以及代码里遗传算法训练的遗留痕迹。"\ncategory: "游戏开发"\ncategoryId: "game"\ndate: "2025-09-22"\nauthor: "徐宁"\nproject: tetris-ai\nseries: tetris-notes\n---\n\nTetris AI 课设的 AI 不是神经网络，而是经典 **启发式搜索**。对当前方块枚举所有合法 **列与旋转**，模拟落子后算场地分数，取最优。实现集中在 `PD.cpp`，头文件 `Tetris.h` 里还留着 **遗传算法调权重** 的数据结构。架构分层见 [Game 与 Tetris 拆分](/blog/tetris-ai-sfml-architecture)。\n\n## 一、托管模式怎么接入\n\n`Tetris::Logic()` 入口大致如下。\n\n```cpp\nif (AIChange) { AI = !AI; if (AI) getPosition(); … }\nif (AI == false) { traditonLogic(); … }\nelse { logicAI(); }\n```\n\n左 Alt 与右 Alt，P1 与 P2，切换 `AIChange`，切到托管时立刻 `getPosition()` 定好当前块落点。`gameInitial()` 里 `player2.AI = true`，默认 **玩家 2 为 AI**。托管走 `logicAI()`，**不走** 消行动画分支，每逻辑步近似落一颗。\n\n`logicAI()` 流程分五步。\n\n1. `hardDropFunc()` — 按 `getPosition` 结果硬降  \n2. 把活动块写入 `Field`  \n3. `newCheckLine()` — 简化版消行，无动画  \n4. `newShapeFunc()` — 生成下一块  \n5. 若未结束，调用 `getPosition()` 与 `shadow()` 为下一颗做准备  \n\n游戏结束时把分数与 `Field` 写入本地日志文件，方便对照 AI 是否堆坏了。\n\n## 二、落点搜索 getPosition()\n\n核心思想是 **穷举但搜索空间很小**。\n\n- 水平最多 10 列，从最左扫到最右  \n- 旋转最多 4 态，`STATE` 宏为 4  \n- 合计约 40 次模拟，每帧可接受  \n\n算法步骤如下。\n\n1. 把当前块移到最左  \n2. 外层循环列 i = 0..9，内层循环旋转 j = 0..3  \n3. 每次调用 `initialAI()`，复制 Field 到 tempField 并模拟硬降  \n4. 若 `hitTest()` 合法，则 `scores[i][j] = calculate()`  \n5. 每轮内 `newRotate()`，列循环结束后 `newXMove()` 右移一格  \n6. 取 scores 最大，还原旋转次数与水平位移  \n\n辅助函数在 `PD.cpp`。\n\n- `newRotate` 与 `newXMove` — 在 **不污染真实 Field** 的前提下动 `currentSquare`\n- `newHardDrop` — 落到底，结果块位置记在 `shadowSquare`，盘面写入 `tempField`\n- `initialAI` — 复制 `tempField` 自 `Field`，再调用 `newHardDrop`\n\n搜索用的是 `tempField` 与 `shadowSquare`，真实 `Field` 只在 `logicAI` 落子时才改。\n\n## 三、六个特征在算什么\n\n`calculate()` 从 `tempField`，已模拟落子，读取特征，与学术界常见的 **Dellacherie 与 El-Tetris 启发式** 同类。\n\n| 函数 | 特征 | 直觉 |\n|------|------|------|\n| `getLandingHeight()` | 落点高度 | 越高越危险 |\n| `getRowsEliminated()` | 消行数 × 参与消行的块数 | 鼓励清行 |\n| `getRowTransition()` | 每行空与满切换次数 | 锯齿少、平整 |\n| `getColumnTransition()` | 每列相邻行切换次数 | 同上，纵向 |\n| `getHoleNum()` | 空洞数 | 被上方块盖住的空格 |\n| `getWellSum()` | 井深度和 | 两侧有墙的单列深坑 |\n\n`getHoleNum` 对每一列从上往下找第一个块，再统计其下方连续空格，被盖住的部分累加。\n\n`getWellSum` 统计左右有墙或贴边的井，按深度累加——深井不利于后续填平。\n\n代码里还注释了另一套特征，`getTotalHeight`、`getHeightDifferences`，是调参过程中的备选维度。\n\n## 四、加权评分与硬约束\n\n当前运行版本使用 **固定浮点权重**，注释写明来自训练结果。\n\n```cpp\nsum = landingHeight * -4.500158825082766\n    + rowsEliminated * 3.4181268101392694\n    + rowsTransition * -3.2178882868487753\n    + columnTransition * -9.348695305445199\n    + holeNUm * -7.899265427351652\n    + wellSum * -3.3855972247263626;\n```\n\n任一特征计算异常为负值时，返回 `-DBL_MAX`，该落点直接淘汰。\n\n额外 **硬约束** 如下。\n\n```cpp\nif (tempField[2][5] != 0 || tempField[2][6] != 0\n || tempField[3][5] != 0 || tempField[3][6] != 0)\n    return -DBL_MAX;\n```\n\n即出生区中心偏右两列上方不能顶死——避免 AI 把堆叠顶进 spawn 区导致秒死。这是课设里很实用的 **domain rule**，和纯数学特征互补。\n\n## 五、遗传算法，结构在，训练默认关\n\n`Tetris.h` 定义种群规模与权重数组。\n\n```cpp\n#define NUM_WEIGHTS 6\n#define POPULATION_SIZE 1000\nstruct GAINFO {\n  int generationNum, count, bestFitness;\n  float bestWeights[NUM_WEIGHTS];\n  int allFitness[POPULATION_SIZE];\n  float allWeights[POPULATION_SIZE][NUM_WEIGHTS];\n};\n```\n\n`logicAI()` 末尾大段注释掉的逻辑，每局结束把 fitness 记入种群，满 1000 局写代际文件、繁殖下一代。也就是说 **原本打算用 GA 自动搜权重**，实际交付版改为 **手写已收敛的系数**，减少答辩时 AI 每局表现漂移的风险。\n\n若重新打开训练链路，需要定义 fitness，多局自动跑满种群，再用输出文件替换 `calculate()` 里常量。\n\n**搜索策略，穷举落点，和评估函数，权重，可以分开迭代**——先保证 `getPosition` 正确，再只调 `calculate`。\n\n## 六、手动模式 vs AI 的差异\n\n| 维度 | 手动 `traditonLogic` | AI `logicAI` |\n|------|---------------------|--------------|\n| 输入 | 键盘累积 `dx`、旋转标志 | 无键盘，直接最优落点 |\n| 重力 | `timer > delay` 逐步下落 | 每步硬降 |\n| 消行 | `checkLine` 与动画与 `clearLine` | `newCheckLine` 立即压缩 |\n| Hold | 支持 | 托管分支未调用 `holdFunc` |\n\nAI 强度不仅来自权重，也来自 **反应速度与无动画等待**。双打时 P2 AI 往往碾压手动 P1——截图里一侧赢一侧输很典型。\n\n若要公平对战，可以降低 `gameLogic` 调用频率、给 AI 加 `delay`，或让 AI 也走 `traditonLogic` 仅自动设 `dx` 与旋转。\n\n## 七、复杂度与可改进方向\n\n- 时间约为 40 次模拟乘以特征计算，特征又对 10×20 格子线性扫描，毫秒级可接受\n- 空间 `tempField`、`scores[10][4]`，无动态分配  \n\n常见增强，课设未做。\n\n1. **看一块以上** — 把 `nextShapeNum` 纳入搜索，深度 2  \n2. **Hold 决策** — 分支比较现在 Hold 与不 Hold 的期望分  \n3. **权重再训练** — 恢复 GA 或网格搜索，针对 Bag7 与 Hold 规则重拟合  \n\n## 小结\n\nTetris AI 的智能 = **小空间完全搜索与六维场地启发式**。理解时抓住两条线，`getPosition` 负责 **在哪落**，`calculate` 负责 **好不好**。美术与双打玩法来自课程模板，本人课设增量主要是这条评估链与 `logicAI` 接入。\n\n标签：工程化\n';export{n as default};
