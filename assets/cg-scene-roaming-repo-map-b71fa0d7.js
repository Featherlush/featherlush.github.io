const n=`---
title: "图形学课设的软光栅约束与仓库梳理"
excerpt: "场景漫游大作业要求在 CPU 上自写管线，不用 OpenGL 等现成渲染库，官方 hducg 只是接口模板参考。"
category: "计算机图形学"
categoryId: "graphics"
date: "2024-12-05"
author: "徐宁"
project: scene-roaming
series: cg-notes
---

2024 年 12 月的 **场景漫游** 大作业，和平时抄个 Demo 能转不太一样。课程要求 **在 CPU 上做软光栅**，**不能用 OpenGL、DirectX 等现成三维渲染库** 把场景画出来。老师下发的 **hducg 官方示例只是模板参考**——对齐讲义里的函数接口、对照每周实验怎么分层，**不是**把官方包原封交上去就算完成。

这篇记 **课设到底要交什么能力**，不涉及工程目录怎么摆。框架与光栅细节见 [SDL 主循环](/blog/cg-hducg-sdl-framework)、[裁剪与 Phong](/blog/cg-rasterization-depth-phong)。

## 一、允许什么、禁止什么

| 层面 | 课设里的做法 |
|------|----------------|
| **三维显示** | 自己写，模型变换、视图、投影、裁剪、光栅化与着色 |
| **窗口与输入** | SDL2 开窗、事件、把内存里的像素刷到屏幕 |
| **模型 I/O** | 读 OBJ/MTL、贴图文件，算数据输入，不替代渲染 |
| **禁止** | 调用 GPU 渲染 API 完成场景绘制；把光线追踪库、现成场景引擎当黑盒交差 |

SDL 只是显示器，真正打分点在 **矩阵、裁剪、消隐、光照** 是否是你写的。同学用 OpenGL 一键 \`glDrawElements\` 画面更亮，但那不是这条作业线。

## 二、官方模板扮演什么角色

hducg 按 **教学周次** 在头文件里列出接口，画线、裁剪、填充、\`drawScene\` 等。每周实验逐步实现其中一块；大作业是在 **同一套接口约定下**，接上外部 OBJ 模型、透视相机与键盘漫游、背面剔除，以及尝试深度与 Phong。

官方周练代码，ZBuffer 小测、四视口、光线追踪静帧，**帮助理解单点**。大作业要自己 **横向拼成一条可交互的管线**。模板是 **参考书**，不是成品答案。

## 三、一条帧的大致数据流

单帧从输入到上屏，大致经过这些步骤。

1. 键盘或鼠标更新相机位姿  
2. 加载模型，**应在初始化阶段做一次**，我当初每帧加载是 bug  
3. \`clearPixels\` 清空画布  
4. 对每个三角形，模型矩阵乘顶点，经世界到相机、近远裁剪、投影到 NDC、视景体裁剪、视口映射到像素、背面判断、Phong 算色，最后填充多边形  
5. 把 \`pixels\` 上传到 SDL 纹理并呈现  

和 GPU 管线 **概念同构**，只是每一步落在 \`setPixel\` 和手写矩阵上。第一次跑通时屏幕里哪怕只有一个 **歪三角形**，也比看一张 OpenGL 截图更能建立坐标系直觉。

\`\`\`cpp
void setPixel(int x, int y, unsigned int color) {
    if (x < 0 || x >= CANVAS_WIDTH || y < 0 || y >= CANVAS_HEIGHT) return;
    pixels[y * CANVAS_WIDTH + x] = color;
}
\`\`\`

## 四、周练与课设的落差

单周往往只考 **一个技能点**。画线算法能画对，Z-buffer 实验里深度测试能工作，四视口能分屏画静态场景，光线追踪能出一张图——各自独立都能过关。

课设要把 **OBJ、交互、光照与多视口设想** 塞进同一帧缓冲。我当时的捷径是先把 \`drawTriangles\` 拼起来能漫游，**深度测试没真正写进填色循环**，Phong 里光照方向也抄错——能跑，但画面灰、闪、穿模。

**纵向作业过 ≠ 横向集成过**。整合缺的是工程时间与胆量，不是再多背一个公式。

## 五、和之前图像课的分工

同年早些时候做过 **灰度点运算** 作业，在二维像素上练映射 \`s = T(r)\`。图形学课把同一套像素思维抬到三维，多了矩阵链和裁剪，但 **最终仍要落到 setPixel**。

读任何三维 API 的投影矩阵时，会想起自己写的 \`ProjectionPerspective\`—— **API 是管线的外壳，数据流才是内核**。

## 小结

课设约束可以概括成一句话，**CPU 软光栅，SDL 只当显示器，hducg 对齐讲义接口**。后续 [框架层](/blog/cg-hducg-sdl-framework) 与 [光栅细节](/blog/cg-rasterization-depth-phong) 分别展开实现；[复盘篇](/blog/cg-scene-roaming-lessons) 记交作业时的感触。

标签：工程化
`;export{n as default};
