const n=`---
title: 图像处理点运算作业的一点感触
excerpt: 2024 年 10 月第一次系统做灰度变换，从会调 matplotlib 到知道 gamma 在改什么。
category: 成长随笔
categoryId: career
date: 2024-10-22
author: 徐宁
project: gray-level-transform
series: ip-notes
---

2024 年 10 月交图像处理点运算作业时，代码就一个脚本、一张样例图、弹窗六张灰度图。体量小，但我记得第一次把 **γ=2.2** 和 **γ=0.5** 并排看时，对伽马校正不再是课本名词。公式实现见 [另一篇](/blog/ip-gray-level-point-ops)。

## 一、点运算教会的是映射，不是库

PIL 读图、Matplotlib 画图，工具都很熟。作业真正要交的是 **你能写出 s 和 r 的关系，并解释画面为什么变亮或变暗**。

对数、幂律、负片：每张子图对应 **一条曲线**。后来做可视化或调相机曝光，底层都有 **非线性映射** 的影子，只是从灰度一个通道变成 RGB 或多参数。

\`\`\`python
gamma = 2.2
power_image = c * (image ** gamma)
\`\`\`

## 二、常数 c 不是凑数

一开始觉得 \`c = 255 / log(1+max)\` 是老师给的魔法数字。自己把 \`c\` 去掉或写死 255 后，图 **全白或全黑**，才理解 **点运算之后还要把动态范围缩回可显示区间**。这和深度学习里 **归一化到 0 与 1 区间** 是同一类问题，只是作业用 0：255 和 \`uint8\`。

## 三、逆变换与理论上可逆

对数变换后再做逆对数，和原图 **不完全一样**，因为中间 \`uint8\` 截断和浮点舍入。**图像处理里很多操作是近似可逆**；写报告时若说完全恢复，最好附带差值图或说明误差来源。

## 四、和后续三维课设的衔接

同年 12 月还要交图形学大作业，从二维点运算到三维 MVP，跨度不小。但两者有一个共同点，**最终都要落到像素上**。先在灰度上搞清映射 \`s = T(r)\` 这条关系，再进矩阵链，心态上会顺一点。

| 维度 | 图像处理 | 图形学 |
|------|----------|--------|
| 空间 | 2D 灰度 | 3D 场景 |
| 核心 | 点运算 \`s = T(r)\` | MVP 与光栅与光照 |
| 工具 | NumPy 与 Matplotlib | CPU 软光栅与 SDL |

## 五、若重做会加什么

1. 一张图里 **绘制变换曲线 T(r)** 与子图并列，报告更直观  
2. 命令行参数传入 \`gamma\`、\`n\` 和图片路径  
3. 输出 PNG 到文件夹，方便交作业不用现场截图  

当前版本保留 **课提交时的简洁脚本**，作为入门快照即可。

## 小结

这份作业不会出现在简历最显眼的位置，但适合放在作品集里说明 **图像链路上最基础的点运算我亲手推过一遍**。小作业的价值在 **曲线直觉**，不在代码行数。

标签：工程化
`;export{n as default};
