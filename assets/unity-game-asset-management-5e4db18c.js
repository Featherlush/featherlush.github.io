const e='---\ntitle: "Unity 塔防课里的 Resources 与 StreamingAssets 分层"\nexcerpt: "网课塔防 Demo 里的资源怎么放、怎么加载、怎么和 JSON 表对齐，ArtRes 与 Resources 分工，以及跟练时的路径踩坑。"\ncategory: "游戏开发"\ncategoryId: "game"\ndate: "2025-12-28"\nauthor: "徐宁"\nseries: unity-notes\n---\n\n网课塔防 Demo 没有上 AssetBundle 或 Addressables，而是用 **文件夹约定、`Resources.Load` 与 StreamingAssets 配表** 完成资源管理。这篇专门讲这一块。编辑器与生命周期见 [Unity 初学](/blog/unity-beginner-first-steps)，加载之后谁消费这些资源见 [架构篇](/blog/unity-tower-defense-framework)。\n\n**说明** — 目录结构与 JSON 字段来自课程资料。我本地复现时，大量时间花在路径字符串是否与文件夹一致，以及哪些资产必须进 Resources。\n\n## 一、Unity 里资源指什么\n\n在 Project 窗口里看到的每一项 — 贴图、音频、Prefab、动画控制器、材质、场景 — 都是 **资产 Asset**。运行时不能直接用磁盘路径读 `.prefab`，编辑器里可以 `AssetDatabase`，打包后不行。必须通过 Unity 的加载 API，且资产要放在会被打进包体、并建立索引的位置。\n\n本 Demo 用到的两类特殊文件夹：\n\n| 文件夹 | 运行时 API | 典型内容 | 是否整包进玩家包 |\n|--------|------------|----------|------------------|\n| `Assets/Resources/` | `Resources.Load<T>(path)` | 要动态实例化的 Prefab、UI、音效、特效、表用的小图 | 是，Resources 下全部参与构建，慎用子目录泛滥 |\n| `Assets/StreamingAssets/` | `Application.streamingAssetsPath` 与文件 IO | 默认 JSON 表、可被玩家存档覆盖的出厂数据 | 是，原样拷贝到包内，路径因平台而异 |\n\n场景里直接摆放的地形、建筑、灯光多在 `Assets/Scenes`、`Assets/ArtRes`，随场景引用进包，不需要也不应该再 `Resources.Load` 一遍。\n\n## 二、顶层分工\n\n跟练工程里可以粗分为几层：\n\n- **Scripts/** — 逻辑，不直接当美术资源加载\n- **Scenes/** — BeginScene、GameScene1 与 GameScene2\n- **ArtRes/** — 课程打包的第三方环境包，墓地、雪地、塔防素材等。场景里引用，或复制后做成 Prefab 再挪到 Resources\n- **Resources/** — 运行时按字符串动态加载的一切\n  - `UI/` — Canvas、各 Panel Prefab\n  - `Role/` — 英雄 Prefab\n  - `Monster/` — 怪物模型 Prefab，逻辑组件运行时 Add\n  - `Tower/` — 炮塔 Prefab\n  - `TowerImg/` — 造塔按钮图标 Sprite\n  - `eff/` — 开火、命中特效\n  - `Animator/` — 怪物 AnimatorController 与 OverrideController\n  - `Music/` — 音效 AudioClip\n  - `imgScene/` — 选关预览图\n- **StreamingAssets/** — RoleInfo、SceneInfo、MonsterInfo、TowerInfo 等 JSON\n\n**ArtRes** 体积大、层级深，主要是关卡美术与场景搭建。讲义要求把会在代码里 `Instantiate` 的路径收敛到 `Resources` 下，并用 **不含 Resources 前缀、不含扩展名** 的字符串在 JSON 里引用。\n\n例如 `RoleInfo.json`：\n\n```json\n{"id":1,"res":"Role/1","hitEff":"eff/1","atk":2,...}\n```\n\n对应资产文件 `Assets/Resources/Role/1.prefab`，加载写法：\n\n```csharp\nResources.Load<GameObject>("Role/1");\n```\n\n**规则** — `Resources.Load` 的路径 = 从任意 `Resources` 文件夹往下到资产名，中间用 `/`，不要 `.prefab`。\n\n## 三、Resources 加载在工程里的用法\n\n跟练代码里出现的类型与用途：\n\n| 类型 | 调用处 | 路径来源 |\n|------|--------|----------|\n| `GameObject` | `UIManager`、`GameLevelMgr`、`MonsterPoint`、`TowerPoint` | 固定或 JSON 的 `res` |\n| `Sprite` | `ChooseScenePanel`、`TowerBtn` | `imgRes`、`imgScene/x` |\n| `RuntimeAnimatorController` | `MonsterObject.InitInfo` | JSON `animator` |\n| `AudioClip` | `GameDataMgr.PlaySound` | 如 `Music/Wound` |\n\n统一模式：\n\n```csharp\nGameObject obj = Instantiate(\n    Resources.Load<GameObject>(path),\n    position,\n    rotation\n);\n```\n\n注意三点：\n\n1. **`Load` 返回 null** 时 `Instantiate` 会抛异常。讲义用 try-catch 包音效，我本地排错时先在 Project 里搜路径是否真存在。\n2. **Resources 不参与地址随机化**，但 **整个 Resources 树都会进包**。课程 Demo 体量可控，商业项目通常会迁到 Addressables。\n3. 同类资源用文件夹语义分，`UI/`、`Role/`、`Monster/`，避免全堆在 Resources 根目录。\n\n## 四、StreamingAssets 与 JsonMgr\n\n**配置表** — 只读默认、策划可改出厂值 — 放 StreamingAssets。读档优先级如下：\n\n```csharp\n// 先 StreamingAssets，再 persistentDataPath，都没有则 new T()\nstring path = Application.streamingAssetsPath + "/" + fileName + ".json";\nif (!File.Exists(path))\n    path = Application.persistentDataPath + "/" + fileName + ".json";\n```\n\n出厂 JSON 包括：\n\n- `RoleInfo.json`、`SceneInfo.json`、`MonsterInfo.json`、`TowerInfo.json`\n\n**玩家进度** — 解锁英雄、金币、音量 — 由 `JsonMgr` 写到 `persistentDataPath`。\n\n这样设计的好处：\n\n- 首次安装读出厂 JSON\n- 玩家购买英雄后 `SavePlayerData()`，下次优先读存档，不改动 StreamingAssets 里的原表\n- 表里的 `res`、`eff`、`imgRes` 仍是 Resources 路径字符串，实现数据与资产分离\n\n`TowerInfo` 一行同时引用三种资源：\n\n```json\n{\n  "imgRes": "TowerImg/1_1",\n  "res": "Tower/1_1",\n  "eff": "eff/Fire1"\n}\n```\n\nUI 用图、场上实体用 Prefab、开火用特效。一张表驱动三种 Load，扩塔时主要改 JSON 和 Resources 里对应文件，而不是改 C# 分支。\n\n## 五、预制体、Override 与动画资源\n\n怪物 Prefab 在 `Resources/Monster/`，动画在 `Resources/Animator/Monster/`：\n\n- 基类 `Aggresive.controller`\n- `Crawl.overrideController` 等 **Override Controller** 换皮同一套状态机\n\n`InitInfo` 里：\n\n```csharp\nanimator.runtimeAnimatorController =\n    Resources.Load<RuntimeAnimatorController>(info.animator);\n```\n\nAnimator 控制器也可以当资源动态换，适合同骨骼多外观。动画 **事件** `AtkEvent` 绑在 Controller 的 Clip 上，和 Prefab 路径是两条线。改控制器资源时要打开 Animation 窗口检查事件是否还在。\n\n## 六、音效与短生命周期对象\n\n`PlaySound` 不走对象池，而是：\n\n```csharp\nGameObject musicObj = new GameObject();\nAudioSource a = musicObj.AddComponent<AudioSource>();\na.clip = Resources.Load<AudioClip>(resName);\na.Play();\nDestroy(musicObj, 1f);\n```\n\n资源管理角度 — AudioClip 在 Resources/Music，播放时临时 GameObject，播完销毁。简单，但高频音效会 GC。网课阶段接受，以后可换 AudioMixer 加池化。\n\n## 七、跟练时常见的资源问题\n\n| 现象 | 常见原因 |\n|------|----------|\n| `Load` 为 null | 路径多了 `Resources/` 前缀或写了 `.prefab`；资产不在 Resources 下 |\n| 图显示空白 | Sprite 模式不对；`imgRes` 与 `TowerImg` 文件名不一致 |\n| 怪物没动画 | `animator` 字段路径错；Override 没指到基类 Controller |\n| 音效不响 | `Music/xxx` 与文件夹不一致；或 `mute` 被设置面板关掉 |\n| 打包后 JSON 读不到 | StreamingAssets 在部分平台要用 `UnityWebRequest` 读；本 Demo  Windows standalone 直接 `File` 可读，课程以 Editor 加 Win 打包为主 |\n\n## 八、和正规资源管理的距离\n\n网课 deliberately 选 **Resources 与 JSON**，为的是少讲构建管线，先讲实例化、表驱动、存档路径，和以后学的 ScriptableObject、AssetBundle、Addressables 形成对比。\n\n我目前的理解层次：\n\n- **场景引用** — 静态关卡、大环境，ArtRes 加 Scenes\n- **Resources.Load** — 动态生成、UI、按表刷怪造塔，本 Demo\n- **StreamingAssets** — 可替换的出厂数据，与 persistentDataPath 分工\n- **Addressables** — 大项目分包、按需下载，未在本课出现\n\n资源分层搞清之后，刷怪、索敌、动画事件那篇读起来会顺很多 — 那些逻辑大量依赖 JSON 里的路径字符串和 Resources 里的 Prefab 一一对应。\n';export{e as default};
