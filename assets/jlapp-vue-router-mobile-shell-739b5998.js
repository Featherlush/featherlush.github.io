const n="---\ntitle: Hash 路由与移动壳层怎么搭\nexcerpt: 四十多条路由、四级 Tab、嵌套 children，还要对接 HBuilder 打包。第一次独立搭移动壳层时，Hash 模式、meta 驱动 Tab 和 children 分层是怎样定下来的。\ncategory: 前端开发\ncategoryId: frontend\ndate: 2024-07-05\nauthor: 徐宁\nproject: jiuli-app\nseries: vue-notes\n---\n\n2024 年 7 月，我在究理 APP 里第一次独立搭完整移动壳层：底部四 Tab 主页、分类、讨论、我的，外加闯关答题、社区发帖和个人中心子页，路由表超过四十条。这篇只谈 Vue Router 4 加 Hash 模式怎么组织。\n\n## 为什么选 Hash 而不是 History\n\n移动壳用 HBuilder 打包成 Android APK，本质是 WebView 加载本地 `dist/index.html`。Hash 路由 `#/home` 不依赖服务端重写，文件协议或任意部署路径下都能跳转，竞赛交付更稳。\n\n```javascript\nconst router = createRouter({\n  history: createWebHashHistory(),\n  routes,\n})\n```\n\nHistory 模式在纯静态托管时要配 fallback，APK 内一般没必要折腾。\n\n## 路由分层：四大 Tab 与业务子页\n\n一级 Tab 用 `meta.showTabbar: true` 标记，全局 TabBar 读当前路由 meta 决定是否展示：\n\n| 路径 | 组件 | 说明 |\n|------|------|------|\n| `/` | Home | 首页聚合 |\n| `/category` | Category | 分类入口，内嵌三级导航 |\n| `/chat` | Chat | 学习讨论区 |\n| `/my` | My | 个人中心 |\n\n登录注册链在 Tab 外：`/login`、`/register`、`/choose` 选学生或教师与年级。\n\n内容详情用动态参数：`/article/:id`、`/expand/:id`、`/post/:id`。\n\n闯关链路用路径拼 id，项目里写成 `/answer:id` 等形式，依赖 Hash 解析习惯，改路由时要对照实际跳转写法。\n\n## 分类页：三层嵌套 children\n\n`/category` 是整套路由里最深的结构：\n\n1. `Category` 容器与 `redirect: '/Nav1'`\n2. `Nav1`、`Nav2`、`Nav3` 三个横向 Tab，课程、文章、动态，各自 `meta.title`\n3. 每个 Nav 下再挂 `Nav11`：`Nav14` 等叶子页\n\nlayout、redirect 与 children 和后台侧栏类似，只是横向 Tab 用 Element Plus，内容区仍是 Vant 移动组件。\n\n要点：\n\n- **redirect** 保证进入 `/category` 总有默认子页，避免空白：子路由 path 写绝对路径如 `/Nav11`，Hash 模式下菜单跳转直观\n- `meta.title` 留给顶栏，不必每个叶子页重复写标题组件\n\n## 全局入口与登录守卫\n\n`App.vue` 在 `created` 里读 `localStorage` 的 `token`，无 token 或 JWT 过期则 `push('/login')`。这是粗粒度守卫：未用 `router.beforeEach`，所有页面加载前统一拦一次。\n\nJWT 过期在前端用 `atob` 解 payload 的 `exp` 字段判断，无校验签名，仅看时间戳。竞赛项目够用，生产应配合后端 401 与刷新策略。\n\n## Vite 构建与 HBuilder 衔接\n\n`vite.config.js` 里 `base: './'`，保证打包后相对路径在 APK 的 file 协议或应用沙箱路径下资源可加载。`build.minify: false` 与 `sourcemap: true` 是为联调期方便看报错，交付前可按体积分阶段打开压缩。\n\nHBuilder 侧把 `frame/dist` 作为 Web 资源目录，配置启动页与图标即可安装到真机。前端路由全部在 Hash 内完成，不需要为每个 Vue 页面配原生路由。\n\n## 和图表模板项目的差异\n\n| 维度 | 图表模板 chart-view | 究理 APP |\n|------|---------------------|----------|\n| 路由规模 | 侧栏与 20 余 chart 页 | Tab 与嵌套分类、社区、闯关 |\n| 模式 | History Vite dev | Hash APK |\n| 布局 meta | 侧栏菜单表 | `showTabbar` 与分类 `title` |\n\n## 小结\n\n移动壳层的核心是 Hash 适配打包发布、meta 驱动 Tab 显隐、children 表达多级分类。路由表一大，靠 meta 和 redirect 比在每个页面里手写布局判断省事得多。\n\n标签：Vue.js, 移动开发\n";export{n as default};
