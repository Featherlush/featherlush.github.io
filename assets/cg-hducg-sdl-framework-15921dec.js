const n="---\ntitle: hducg 软渲染与 SDL 主循环怎样搭建\nexcerpt: 从 pixels 数组到 SDL_Texture，图形学课设里框架层与 Camera 类各自管什么。\ncategory: 计算机图形学\ncategoryId: graphics\ndate: 2024-12-10\nauthor: 徐宁\nproject: scene-roaming\nseries: cg-notes\n---\n\n场景漫游的 **框架层** 可以分成两块。**hducg** 管画什么、怎么变换，**SDL** 管窗口、输入、把 CPU 像素推到屏幕。官方模板把接口按 **课程周次** 刻在头文件里：画线、裁剪、填充、场景：大作业是在这些钩子上接 **OBJ 与相机漫游**。课设约束与管线总览见 [上一篇](/blog/cg-scene-roaming-repo-map)。\n\n## 一、像素画布，一切绘制的终点\n\n全局维护一块 RGBA 画布，典型尺寸 800×600。\n\n```cpp\nint CANVAS_WIDTH = 800, CANVAS_HEIGHT = 600;\nunsigned int* pixels;  // RGBA8888\n```\n\n`clearPixels`、`setPixel`、`getPixel` 是唯一写屏通道。`mapRGBA` 与 `readRGBA` 负责字节与 32 位颜色打包。\n\n**没有 GPU**，三角形最终就是 `setPixel` 循环。理解这一点后，OpenGL 的帧缓冲就不再神秘：只是硬件帮你批量 `setPixel` 并加了深度测试。\n\n## 二、SDL 侧的主循环\n\n主循环负责初始化窗口与纹理，每帧调用 `drawScene` 填满 `pixels`，再上传纹理并处理输入。\n\n1. `SDL_Init`，`CreateWindow`，`CreateRenderer`，`CreateTexture`，格式 RGBA8888  \n2. `init()` 做场景初始化  \n3. `drawScene(camera)` 填满 `pixels`  \n4. `SDL_UpdateTexture` 把内存块上传  \n5. `SDL_RenderCopy`，`Present`  \n6. `PollEvent`，`handleInput`，必要时再次 `drawScene`  \n\n`handleInput` 绑定如下。\n\n| 按键 | 行为 |\n|------|------|\n| W、S、A、D | 相机前后左右 |\n| Space 与 LShift | 上 与 下 |\n| 鼠标左 与 右 | 前进 与 后退 |\n\n每次移动 **立即重绘整场景**，无脏矩形、无增量更新。800×600 软渲染还能扛，模型面数上去就会卡：课设规模刚好。\n\n较稳妥的做法是用 **Streaming Texture** 把 `pixels` 整块上传，而不是只 `GetWindowSurface` 画一次静态图：否则交互漫游时每帧都要能刷新缓冲区。\n\n## 三、Camera，视图与投影\n\n`Camera` 类把讲义公式封装起来。\n\n| 方法 | 作用 |\n|------|------|\n| `world2eye(p)` | 视图矩阵，世界到相机 |\n| `eye2ndc(pe, ptype)` | 正交或透视投影，再透视除 |\n| `clipLineNearFar` 与 `clipTriangleNearFar` | 相机系近远平面裁剪 |\n| `moveForward` 等 | 改 `eye`/`lookat` 并更新视图矩阵 |\n\n投影参数默认 `n=1, f=10`，窗口 `l,r,b,t` 在构造函数里设。漫游时只动位置， **没做鼠标绕观察点旋转**，所以是平移漫游而非完整 FPS。\n\n`drawLines` 与 `drawTriangles` 走同一条流水线。模型变换 m 乘顶点，经 `world2eye`、`clipNearFar`、`eye2ndc`、`clipLine` 或 `clipPolygon` 处理 NDC 立方体，再 `ndc2viewport`，最后 `drawLine` 或 `fillPolygon`。\n\n课设 **场景漫游** 主要走 `drawTriangles`；`drawLines` 多用于线框或周练遗留。\n\n## 四、场景层 init 与 drawScene\n\n`init()` 里准备基础几何，单位立方体线框、细分球等，供早期实验或调试。\n\n```cpp\nvoid drawScene(Camera& camera) {\n    clearPixels(0x808080FF);\n    drawTriangles(modelTris, camera, viewport, depthBuffer);\n    SDL_UpdateTexture(texture, NULL, pixels, CANVAS_WIDTH * 4);\n}\n```\n\nOBJ 与 MTL **应在初始化阶段加载一次**，我当初每帧加载是性能与逻辑 bug。索引展开成三角形列表后，`drawTriangles` 做透视投影并指定视口；设计上可支持多视口，我交的作业只完整画了其中两块。\n\n## 五、matrix，Vector 与 Matrix\n\n课程自建线性代数，未用 Eigen 等库。\n\n- `Vector` 四维齐次，支持 `dot`、`cross`、`normalize`  \n- `Matrix::View`、`ProjectionOrtho`、`ProjectionPerspective`、`Rotation`、`Translation`  \n\n**第一次手写 4×4 乘法**，错一行矩阵，画面整体飞走，比光栅 bug 更容易查。\n\n## 六、框架设计的优缺点\n\n**优点**\n\n- 接口按周推进，和讲义一一对应  \n- `Camera` 把裁剪平面和投影矩阵收在一起，场景代码可读：SDL Streaming Texture 适合教学用 CPU 渲染  \n\n**缺点，课设亲身体会**\n\n- 全局 `pixels`、全局材质表，难对单函数做单元测试  \n- `drawScene` 职责过重，加载、清屏、绘制挤在一起：周练函数与课设代码 **共用一个大文件**，导航困难  \n- `drawScene` 声明只依赖相机参数，实现却绑死全局相机、材质  \n\n若重做，我会拆成 **应用层、场景层与光栅器** 三个模块，周练单独成小工程。\n\n## 小结\n\n**SDL 管壳，hducg 管管线，Camera 管镜头。** 读懂主循环与 `drawTriangles` 的九步，就覆盖了课设大部分框架。画面为何不理想，见 [光栅化与深度](/blog/cg-rasterization-depth-phong) 与 [复盘](/blog/cg-scene-roaming-lessons)。\n\n标签：工程化\n";export{n as default};
