const n="---\ntitle: 特征相关矩阵的外壳与三种格内图\nexcerpt: 从组件分层、drawChart 管线到下三角、对角与上三角三套绘图器，详解 Feature Correlation 的定制逻辑与踩过的坑。\ncategory: 前端开发\ncategoryId: frontend\ndate: 2025-04-18\nauthor: 徐宁\nproject: electricity-forecast-paper\nseries: d3-notes\n---\n\n电脉智绘论文版图 c **Feature Correlation** 是我开发周期里返工最多的单图。外表是一张 **n×n 对称矩阵**扇形圆角格、轴标签与相关色图例，但 **每一个格子内部的绘图类型由 i,j 决定**，下三角是相关强度色块，对角是单变量微型直方图，上三角是双变量散点。ECharts 的 `heatmap` 只能填一种 cell；论文要三种图共存，这是典型的 **矩阵外壳与异构微图** 定制模式，只能用 D3 手写。Grid 与 Pinia 总线见 [仪表盘 Grid 篇](/blog/electricity-paper-d3-dashboard-grid)。\n\n## 一、组件分层\n\n`FeatureMatrixView.vue` 不是单一 heatmap 组件，而是三层职责拆分。\n\n| 层级 | 职责 | 关键实现 |\n|------|------|----------|\n| L0 容器 | 布局尺寸、数据请求、重绘触发 | Vue card、`ref` 监听、`axios`、`resize` |\n| L1 矩阵外壳 | 格网位置、轴标签、色图例 | 单 SVG、`scaleBand` 双轴、`getCellDimensions` |\n| L2 单元格绘图 | 按 `(i,j)` 画不同微图 | `drawLowerTriangleCell`、`drawDiagonalHistogram`、`drawUpperTriangleCell` |\n\nL2 分支规则：`i > j` 下三角相关色块，`i === j` 对角直方图，`i < j` 上三角散点。\n\n**L1 与 L2 解耦** 是后续能改某一格逻辑而不动全局的关键：轴、边距、`getCellDimensions` 属于外壳；三种 `draw*` 函数只接收 `(svg, i, j, xScale, yScale)` 和已算好的 `rectWidth/rectHeight`。\n\n### 数据流\n\n选中层级树叶子 到 `GET /featureSelection?name=` 到 三块入参：\n\n| 响应字段 | 结构 | 服务于 |\n|----------|------|--------|\n| `data` | `number[][]` 相关矩阵 | L2 下三角 `d = matrix[i][j]` |\n| `value` | `Record<特征名, number[]>` 时序样本 | L2 对角 histogram、上三角 scatter |\n| `name` | `string[]` 特征顺序 | L1 `scaleBand.domain` |\n\n白名单叶子列表：中间层节点没有稳定特征序列，请求会空，**在架构上把可画矩阵限制在叶子**，避免外壳建好却无数据。\n\n## 二、外壳绘图逻辑L1\n\n### 2.1 尺寸，正方形矩阵随左栏宽度伸缩\n\n```javascript\nsvgWidth.value = parentElement.clientWidth\nsvgHeight.value = parentElement.clientWidth  // 故意正方形\n```\n\n左栏 Grid 格窄，矩阵必须 **一维驱动二维**。障碍：首次 `onMounted` 时父级高度可能未稳定，需要 `nextTick` 与 `resize` 再 `drawChart`。\n\n### 2.2 双 scaleBand 与视觉格不等于 bandwidth\n\n```javascript\nconst xScale = d3.scaleBand().domain(name.value)\n  .range([margin.left, width.value - margin.right + 7])\nconst yScale = d3.scaleBand().domain(name.value)\n  .range([margin.top, height.value - margin.bottom])\n```\n\n`scaleBand` 的 `bandwidth()` 是类别占用的步长，论文图里格子之间还有 **扇形间隙**。我用 `getCellDimensions` 反算：\n\n```javascript\nconst tickMargin = (width - margin.right * 1.5\n  - (n - 1) * (xScale(name[1]) - xScale(name[0]))) / 2\nreturn { tickMargin, rectWidth: tickMargin, rectHeight: tickMargin }\n```\n\n这是第一个大坑，**直接用 `xScale.bandwidth()` 画 rect，格子会偏大并互相挤压**；必须按剩余空白平分得到与论文接近的圆角块尺寸。调 `margin.left + 9`、`translate(8, margin.top+5)` 等魔法数，是为了让轴文字与格网对齐。\n\n### 2.3 轴，只要字，不要线\n\n```javascript\n.call(d3.axisTop(xScale).tickSize(0))\n.selectAll('.domain').style('display', 'none')\n```\n\n论文插图轴无刻度线，只有 8px 特征名。D3 默认 `domain` 会破坏扇形干净边缘，必须显式隐藏。\n\n### 2.4 总控函数 drawChart\n\n```javascript\nmatrix.value.forEach((array, i) => {\n  array.forEach((d, j) => {\n    if (i > j) drawLowerTriangleCell(svg, i, j, d, threshold, xScale, yScale)\n    if (i == j) drawDiagonalHistogram(svg, i, j, xScale, yScale)\n    if (i < j) drawUpperTriangleCell(svg, i, j, xScale, yScale)\n  })\n})\ncreateLegend(svg, colors)\n```\n\n注意 **下三角判定是 `i > j`**，矩阵行索引大于列索引，与数学上下三角含对角的写法不同，对角单独第三条分支，避免一格画两次。\n\n重绘前：`d3.select('.matrix').selectAll('*').remove()`，否则切换节点时旧 rect 或 散点叠层。\n\n## 三、三种单元格绘图器L2\n\n### 3.1 下三角，相关色块 与 可读数值\n\n**语义**，`matrix[i][j]` = 特征 `name[i]` 与 `name[j]` 的 Pearson 相关。\n\n**色标策略**与图例一致但实现很省：\n\n```javascript\nconst valueExtent = d3.extent(matrix.value.flat())\nconst colorScale = d3.scaleLinear()\n  .domain([valueExtent[0], 0, valueExtent[1]])\n  .range([\n    colors[parseInt(colors.length * 6 / 7)],\n    colors[parseInt(colors.length / 2)],\n    colors[parseInt(colors.length / 7)]\n  ])\n```\n\n`colors` 数组有 **100+ 个手工插值色**，图例用 `linearGradient` 铺满全部 stop；单格填色却只取 **三个索引位置** 做 `scaleLinear` 两端，格内渐变单调但和图例色系一致，且不用每格建 gradient。\n\n**障碍**，相关值动态范围随节点变化，若固定 `[-1,1]` 域，弱相关全灰；用 `extent` 拉伸后，不同节点之间 **色感不可横向比较**，但论文场景是看当前节点谁和用电量最相关，可接受。\n\n**格内文字**，`d.toFixed(2)` 加四向白色 `text-shadow`，否则深红底上黑字看不清。\n\n**与用电量相关的强调**，最后一行或列， 超过阈值 `0.3` 时 stroke 深灰 1.5px，让读者先扫与目标变量强相关的边。\n\n### 3.2 对角，格内微型直方图\n\n**语义**，`originalData[name[i]]` 该特征在时间窗内的分布，回答相关数字背后变量长什么样。\n\n**局部坐标系**，每个对角格是独立 `g`，原点左上角，宽高 = rectWidth × rectHeight。\n\n**障碍 1**，样本数不固定，条宽随窗口拉长变极细，几乎成发丝状 histogram，论文图尺寸下仍要能看出有无尖峰，靠 `rx:2` 圆角条和饱和蓝。\n\n**障碍 2**，负值过滤后空数组直接 `return`，该对角格空白，比画错比例尺好。\n\n**障碍 3**，y 轴必须从 0 起，避免小波动被放大成假尖峰。\n\n用了 `enter-update-exit` + `transition` 200ms，为后续若做窗口动画留口子；论文版实际每次全量 `remove` 重绘。\n\n### 3.3 上三角，格内散点\n\n**语义**，两特征样本配对，看 **线性相关以外的形态**弯曲、离群。\n\n**每格独立线性比例尺**，同一矩阵内上三角各格子的坐标轴含义不同，这是 small multiples 的代价。不能共用全局 x 或 y，否则量级差会把点挤成一条线。\n\n点半径 `0.5`，描边 `0.1`：在 ~25px 格子里超过 1px 就会糊成色块。\n\n**长度校验**，`xData.length !== yData.length` 时 `console.warn` 并跳过，后端拼接错位时避免画斜条假相关。\n\n## 四、图例层\n\n`createLegend` 在矩阵右上画 100+ stop 的 `linearGradient` 条 与 Positive、Negative 与 Correlation 文案。与单格 `colorScale` 的三点采样 **逻辑一致、实现分离**，改 `colors` 数组时要同时检查图例与 `drawLowerTriangleCell`。\n\n曾注释掉 `axisBottom` 刻度尺，说明迭代中试过渐变条加数字刻度双编码，最终论文图只保留色条。\n\n## 五、与系统的衔接\n\n```javascript\nwatch(() => treeNodeStore.selectedNodeName, () => {\n  if (list.includes(treeNodeStore.selectedNodeeName)) {\n    getfeatureSelection(...)\n  }\n})\n```\n\n`matrixRendered` 控制说明文案仅在成功绘制后显示，避免空 SVG 时仍占版面。\n\n## 六、定制图表逻辑总结\n\n| 步骤 | 做法 |\n|------|------|\n| 1. 定外壳几何 | Grid、band 或 tree layout，只解决格子或节点放哪 |\n| 2. 定单元类型函数 | 每个格一种 `drawXxx(svg, cellBox, dataSlice)` |\n| 3. 局部比例尺 | 微图内部 `scaleLinear` 与 `scaleBand`，不共享轴除非业务要求 |\n| 4. 容器 g 与 transform | `translate(cellX, cellY)` 把坐标系原点移到格左上角 |\n| 5. 全量重绘 | `selectAll('*').remove()` 或清空容器，避免 Vue 重渲染与 D3 双 DOM |\n| 6. 尺寸跟容器 | `clientWidth`、`clientHeight` 与 resize，别写死论文截图像素 |\n\nHierarchy View 是树外壳加 Glyph 与 Petal 两种节点图；Time Series View 是折线区加日历格区双分区；Evaluation View 是 band 矩阵加居中宽度条，同一思路的不同外壳，分别在 [Hierarchy 篇](/blog/electricity-paper-d3-petal-hierarchy) 与 [时序评估篇](/blog/electricity-paper-d3-timeseries-evaluation) 展开。\n\n## 七、开发障碍清单\n\n1. **bandwidth ≠ 视觉格宽**，调了很久 `tickMargin` 公式才对齐论文扇形间隙\n2. **三色采样 vs 百色图例**，图例示连续谱，格内为可读离散映射\n3. **histogram 条数爆炸**，长窗口下对角格信息密度过高，只能靠整体缩小左栏或接受纹理化\n4. **散点不可跨格比较**，上三角只看形态，强弱仍以下三角数字+色为准\n5. **正方形 SVG 在极窄屏**，轴标签重叠，8px 字是硬上限\n6. **Store 字段拼写**，`selectedNodeeName` 与 `selectedNodeName` 并存，watch 绑错会导致树点了矩阵不刷\n7. **异步与 loading**，`message.loading` 包裹请求，慢接口时避免用户以为矩阵坏了\n\n## 小结\n\nFeature Correlation 的价值不在画了一张相关矩阵，而在 **用统一矩阵坐标系，把三种统计学视图塞进同一扫视路径**，下三角看系数，对角看边际分布，上三角看联合形态。外壳是 D3 的轴与格网；内核是三个按 `(i,j)` 分发的绘图器。掌握这套 **外壳 与 异构微图** 范式后，论文里其它看起来不像标准图表的块，其实都可以拆成同一结构。\n\n标签：D3.js, 数据可视化\n";export{n as default};
