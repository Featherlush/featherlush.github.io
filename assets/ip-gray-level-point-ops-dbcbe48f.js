const n="---\ntitle: 灰度点运算公式怎样落到 NumPy\nexcerpt: 图像处理课作业里的对数、幂律、负片等点变换如何用数组一次性算完，常数 c 和 gamma 各自起什么作用。\ncategory: 图像处理\ncategoryId: image\ndate: 2024-10-18\nauthor: 徐宁\nproject: gray-level-transform\nseries: ip-notes\n---\n\n2024 年 10 月的图像处理作业很朴素，一张灰度图，五种 **点运算**，Matplotlib 拼成六宫格。没有卷积、没有分割，只有 **输入灰度 r 到输出灰度 s** 的映射。这篇记公式怎么落到 NumPy 上；课设背景见 [项目页](/projects/gray-level-transform)。\n\n## 一、点运算是什么\n\n**点运算** 不改变像素之间的空间关系。输出图像第 i 行第 j 列像素只依赖输入图像同位置像素，有时还依赖全局常数，如 `c`。\n\n与 **邻域运算** 对比如下。\n\n| 类型 | 依赖 | 本作业 |\n|------|------|--------|\n| 点运算 | 单像素灰度 | ✅ 对数、幂律、负片 |\n| 邻域运算 | 周围像素 | 未涉及 |\n\n读入时用 PIL `convert('L')` 得到 **单通道** `uint8` 数组，形状为高度 H、宽度 W，值域 0：255。\n\n## 二、对数与逆对数\n\n**对数变换** 用于 **扩展暗部、压缩亮部**，动态范围大的图更常用。公式如下：\n\n```\ns = c · log(1 + r)\n```\n\n实现里先算缩放常数，再对整个数组做元素级运算。\n\n```python\nc = 255 / np.log(1 + np.max(image))\nlog_image = c * np.log(1 + image + 1e-10)\n```\n\n`1 + r` 保证 log 自变量为正；`1e-10` 防数值上 `r=0` 的问题。`c` 把最大值拉到约 255，方便 `uint8` 显示。\n\n**逆对数** 用同一 `c` 做指数还原。\n\n```python\ninverse_log_image = np.exp(image / c) - 1\n```\n\n逆变换的输入应是 **对数变换后的灰度**，浮点再 cast，不是原图。作业里对 `log_img` 调用 `inverse_log_transformation` 用来 **验证映射是否可逆**；因舍入与 `uint8` 截断，和原图会有细微差别。\n\n## 三、幂律与根指数，伽马家族\n\n**幂律变换** 写成：\n\n```\ns = c · r^γ\n```\n\n```python\nc = 255 / np.max(image**gamma)\npower_image = c * (image ** gamma)\n```\n\nγ 大于 1，作业取 **2.2**，曲线下弯， **整体变暗**，亮部被压缩。γ 小于 1 则 **提亮**，暗部扩展。\n\n**根指数变换** 写成 r 的 1/n 次幂，等价于 **γ = 1/n**。作业 `n=2` 即 **γ=0.5**，与幂律 `gamma=0.5` 同一族，只是参数用根号形式表述。\n\n显示器伽马、照片调色里的 gamma 就是这条曲线：课上用一张图把 γ 大于 1 和小于 1 的观感差异跑出来，比背定义有效。\n\n## 四、负片\n\n最简单的一行。\n\n```python\nnegative_image = 255 - image\n```\n\n线性反转，可看作 γ 思路的极端或分段线性点运算。医学底片、简单反相效果都源于此。\n\n## 五、向量化而不是 for 循环\n\n所有变换对 **整个 ndarray** 一次运算，没有 Python 层逐像素循环。这是 NumPy 作业的标准写法。：公式即 **元素级** `+`、`*`、`**`、`np.log`、`np.exp`  \n- 注意 `image**gamma` 时 `image` 为整数类型，NumPy 会提升为浮点再运算：最后 `astype(np.uint8)` 或 `dtype=np.uint8` 截断到显示范围  \n\n若 `c` 用错分母，例如用固定 255 而不用 `max`，整图会 **过曝或全黑**：这是调参时最先检查的。\n\n## 六、展示，六宫格对比\n\n`display_images` 用 `plt.subplot` 排成 2 行 3 列，`cmap='gray'`，中文标题依赖 `SimHei`。作业评分常看 **是否六种变换齐全、对比是否一眼可读**，而不是 UI 炫不炫。\n\n改进空间，作业未做。：同一张图滑条调 γ 实时看图：保存结果到文件而不是只 `show()`  \n- 用 `np.clip` 显式限制再转 `uint8`，避免溢出绕圈  \n\n## 小结\n\n这份作业的技术含量不在工程架构，而在 **把讲义里的映射 `s = T(r)` 写对、用数组算对、用图说清楚**。对数、幂律、负片是后面 **直方图均衡、空间滤波、甚至预处理归一化** 的地基。做作业时的感触见 [另一篇](/blog/ip-homework-reflections)。\n\n标签：工程化\n";export{n as default};
