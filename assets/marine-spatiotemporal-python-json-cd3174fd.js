const n='---\ntitle: "投入产出表到 JSON 与莫兰图"\nexcerpt: "用 Python 从 Excel 抽矩阵生成 link、flow、centrality，前端 molan 与 net 直接 import 这些文件。"\ncategory: "前端开发"\ncategoryId: "frontend"\ndate: "2024-12-02"\nauthor: "徐宁"\nproject: marine-industry\nseries: d3-notes\n---\n\n论文系统没有在线数据库，图表吃的是 `src/assets/` 下大量 JSON。源头是统计年鉴式的 **投入产出 Excel**，用 Python 脚本批处理后再交给 Vue。面板布局见 [Vuex 一屏联动篇](/blog/marine-spatiotemporal-vuex-layout)。\n\n## Python 在链路中的位置\n\n`data.py` 典型逻辑：\n\n```python\ndf = pd.read_excel(\'sample_region.xls\', header=None)\nsub_df = df.iloc[start_row:end_row, start_col:end_col]\n# 遍历矩阵，data.append({"source", "target", "value"})\n# 汇总 node_totals 流入与流出\n```\n\n- 矩阵左下与右上不同位置决定 `source`、`target` 方向\n- 输出 JSON 供 `net.vue` 的 `link_*`、`flow_*` 使用\n\n还有 `node.py` 等脚本按同样套路为不同省份、年份批量生成文件，命名约定如 `centrality/centrality_07某省.json`、`rank/ranked_07某省.json`、`link/link_07某市.json`。前端用 **年份 与 省份** 拼路径或在一组 import 里 switch。\n\n## 莫兰图 molan.vue\n\n右侧上方模块做 **空间自相关** 展示：\n\n- 按钮切换总流入与总流出，与 Vuex flowOption 一致\n- 下拉切换指标，全局莫兰指数、p 值、z 值\n- 中部 D3 **散点图**，`ref="chart"` SVG\n- 底部 **省份表格**，原始值 与 局部莫兰指数\n\n数据源 `inmolan.json`、`outmolan.json` 按年全国序列组织，`yearData` 由当前 `sharedYear` 切片。\n\n页面上有提示，某早年份只有净流出数据。预处理时要和使用方确认 **缺失年份**，否则下拉切到空年会白屏。\n\n## 网络图 net.vue 读什么\n\n- **节点中心度**，`centrality_*` JSON\n- **连边**，`link_*` JSON\n- **流量标注**，`flow_*` JSON\n- **产业名称**，`product.json`\n\n`net.vue` 里 D3 力导向或手动布局，配合 `contorl.vue` 勾选过滤节点。过滤档位用 `el-select` 控制边的可见性。\n\n## 地图 Map.vue\n\n用 `china.json` 筛出沿江等省份，`d3.geoMercator` 与 `geoPath` 画底图。与莫兰、网络 **共享年份与流向**，但几何是静态 GeoJSON，数值上色逻辑相对独立。\n\n## 我踩过的坑\n\n1. **Excel 表头不在第一行**，`header=None` 加手动 `iloc` 切片，改表就要改脚本常量\n2. **JSON 体积**，多省份 × 多年份文件很大，Vite 构建会打包进 bundle，开发机内存要够\n3. **浮点展示**，排名表里中心度用 `toFixed(11)`，论文制图需要，但表格阅读性差，研究向 UI 的常态\n\n标签：Flask, D3.js, 海洋产业\n';export{n as default};
