const n='---\ntitle: 网课塔防的单例管理与 UI 分层\nexcerpt: 2025 年 12 月跟随网课做的 Unity 塔防练习，场景、面板与 JSON 配置怎样串起来，资源与代码均为课程提供，我本地复现。\ncategory: 游戏开发\ncategoryId: game\ndate: 2025-12-18\nauthor: 徐宁\nseries: unity-notes\n---\n\n**说明**：这篇记录的是网课跟练项目，本地工程名 `Game_demo`。**美术、预制体、脚本框架与 JSON 表均来自课程资料**，我的工作是把工程在 Unity 2022.3 里跑通、按讲义补全逻辑，并在复现过程中记笔记。它不是原创商业项目，也不放进本站项目列表。\n\n玩法上是 **第三人称英雄与塔防**：选角色、选关卡、在地图里走位射击或近战，同时在塔位造塔守主塔。和同年 9 月用 C++/SFML 写的 [Tetris 课设](/blog/tetris-ai-sfml-architecture) 不同，这次是第一次系统接触 Unity。编辑器与生命周期可先读 [Unity 初学](/blog/unity-beginner-first-steps)。本篇只谈入口到进关前的架构，刷怪、战斗与胜负留到后面单独记。\n\n## 一、入口极薄\n\n`Main` 挂在 Begin 场景里，`Start` 里一行：\n\n```csharp\nUIManager.Instance.ShowPanel<BeginPanel>();\n```\n\n没有自己的 `Update` 逻辑。课程的设计意图很明确：场景切换、选角、读表都交给管理类与 UI，入口 MonoBehaviour 保持点火器角色。对我这种先写 Web 后端、再碰游戏的人来说，薄入口加厚管理器，比把所有逻辑塞进一个 `GameManager` MonoBehaviour 更容易跟讲义逐步加功能。\n\n## 二、UIManager\n\n`UIManager` 是 **纯 C# 单例**，不是 `MonoBehaviour`。构造时从 `Resources/UI/Canvas` 实例化 Canvas 并 `DontDestroyOnLoad`，保证跨场景 UI 根节点不断。\n\n核心 API：\n\n| 方法 | 行为 |\n|------|------|\n| `ShowPanel<T>()` | 按类名加载 `Resources/UI/{类名}`，挂到 Canvas，调用 `ShowMe()` |\n| `HidePanel<T>(isFade)` | 淡出后 `Destroy` 并从字典移除 |\n| `GetPanel<T>()` | 取已打开面板，供游戏内刷新金币、波次等 |\n\n面板基类 `BasePanel` 用 `CanvasGroup.alpha` 做显隐渐变。`ShowMe` 从 0 淡入，`HideMe` 回调里再销毁。子类只实现 `Init()` 绑按钮事件。\n\nBegin 流程按讲义顺序走下来：\n\n1. **BeginPanel**：点开始\n2. **CameraAnimator**：转镜头\n3. **ChooseHeroPanel**：左右切英雄、解锁、存 PlayerData\n4. **ChooseScenePanel**：左右切关卡预览\n5. **LoadSceneAsync**：按 sceneName 加载战斗场景\n6. **GameLevelMgr.InitInfo**：场景就绪后初始化局内状态\n\n`ChooseScenePanel` 在 `AsyncOperation.completed` 里才调 `GameLevelMgr.InitInfo`，避免场景未加载完就 `Find("HeroBornPos")` 失败。这是跟练时第一个异步加载 completion 再初始化的固定写法。\n\n## 三、GameDataMgr 与 JsonMgr\n\n`GameDataMgr` 同样是静态单例，构造时通过 `JsonMgr` 一次性读入：\n\n- `RoleInfo`、`SceneInfo`、`MonsterInfo`、`TowerInfo`：来自 `StreamingAssets/*.json`，课程默认表\n- `PlayerData`、`MusicData`：玩家进度与音量，可读写到 `persistentDataPath`\n\n`JsonMgr` 的读取策略是讲义重点：\n\n```csharp\n// 1. StreamingAssets 出厂默认\n// 2. persistentDataPath 玩家存档\n// 3. 都没有则 new T()\n```\n\n序列化默认用 **LitJson**，课程自带，`JsonUtility` 作备选。选英雄界面解锁英雄后 `SavePlayerData()`，下次启动仍从持久化路径读。我第一次跑通购买、关游戏、再开仍解锁时，才直观理解 StreamingAssets 与 persistentDataPath 的分工。\n\n`PlaySound` 动态 `new GameObject` 加 `AudioSource`，播完 1 秒销毁。简单粗暴，但对跟练足够。音量、静音来自 `musicData`。\n\n## 四、GameLevelMgr\n\n进战斗场景后，`GameLevelMgr.InitInfo` 负责：\n\n1. `ShowPanel<GamePanel>()`\n2. 按 `nowSelRole` 在 `HeroBornPos` 实例化英雄预制体，`InitPlayerInfo(atk, money)`\n3. `Camera.main` 上的 `CameraMove.SetTarget(hero)`\n4. `MainTower.Instance.UpdateHp` 同步主塔血量到 UI\n\n局内还维护：\n\n- `List<MonsterPoint>`：各刷怪点注册自己\n- `List<MonsterObject>`：当前存活怪物，供塔与玩家索敌\n- `nowWaveNum` 与 `maxWaveNum`：波次 UI\n\n`Clearinfo()` 在返回主菜单或重开时清空列表，避免上一局怪物引用残留。课程把选关前的数据放在 GameDataMgr，这一局里的实体放在 GameLevelMgr，边界清楚。\n\n## 五、几个 Unity 概念落地\n\n| 概念 | 在本 Demo 里 |\n|------|----------------|\n| `Resources.Load` | UI 预制体、英雄/怪物/塔、音效、特效、表里没有的路径字符串 |\n| `SceneManager.LoadSceneAsync` | 两个战斗场景 `GameScene1` 与 `GameScene2` |\n| `DontDestroyOnLoad` | Canvas 根 |\n| `LateUpdate` | `CameraMove` 跟随主角旋转后再摆相机 |\n| Layer | `Monster`、`MainUI` 等，攻击检测用 `LayerMask`，战斗篇细说 |\n\nArtRes 里大量第三方环境包，墓地、自然、塔防素材，都是课程打包好的。我只负责场景里摆路径、NavMesh、触发器，不碰资产版权与原创美术。\n\n## 六、和 Tetris 课设的对比\n\n| | Tetris SFML | 本 Demo Unity |\n|--|-------------|---------------|\n| 循环 | 手写 `while` 与 `pollEvent` | 引擎 `Update` 与 `LateUpdate` |\n| 资源 | CMake 拷 `data/` | `Resources` 与 `StreamingAssets` |\n| UI | 纹理与鼠标区域 | uGUI 与面板管理器 |\n| 寻路 | 无 | `NavMeshAgent` 追主塔 |\n\n两次课设叠在一起，我对谁在一帧里先跑、数据存在哪一层，比单看 Unity 文档清楚得多。架构搭完，下一步是刷怪波次、索敌和动画事件：那部分逻辑量更大，值得单独开一篇。\n';export{n as default};
