const n='---\ntitle: 塔防局内的刷怪、索敌与动画事件\nexcerpt: 塔防 Demo 局内循环，MonsterPoint 波次、NavMesh 追塔、OverlapSphere 与 Raycast 伤害、塔两种攻击模式，课程复现笔记。\ncategory: 游戏开发\ncategoryId: game\ndate: 2026-01-12\nauthor: 徐宁\nseries: unity-notes\n---\n\n接 [架构篇](/blog/unity-tower-defense-framework)。同样声明：**脚本结构与资源来自网课**，下文是我跟写、调试时的理解，不是独立设计的塔防方案。`Resources.Load` 与 JSON 路径约定见 [游戏资源管理](/blog/unity-game-asset-management)，编辑器基础见 [Unity 初学](/blog/unity-beginner-first-steps)。\n\n## 一、MonsterPoint 与 Invoke 驱动的波次\n\n每个刷怪点 `MonsterPoint` 在 `Start` 里：\n\n1. `AddMonsterPoint(this)` 注册到 `GameLevelMgr`\n2. `UpdateMaxNum(maxWave)` 累加总波次到 UI\n3. `Invoke("CreateWave", firstDelayTime)` 延迟首开\n\n一波逻辑分两层：\n\n```csharp\nvoid CreateWave() {\n    int id = monsterIDs[Random.Range(0, monsterIDs.Length)];\n    nowNum = monsterNumOneWave;\n    CreateMonster();\n    maxWave--;\n    GameLevelMgr.Instance.ChangeNowWaveNum(1);\n}\n```\n\n**CreateMonster**：Resources 加载预制体，`AddComponent<MonsterObject>`，`InitInfo`，`AddMonster` 到 GameLevelMgr。`nowNum--` 后，若本波还有怪就 `Invoke CreateMonster(createOffsetTime)`；若本波结束且 `maxWave > 0` 就 `Invoke CreateWave(delayTime)` 开下一波。\n\n`CheckOver()` 返回 `nowNum == 0 && maxWave == 0`，表示这个点不再产怪。全局胜利还要 `GameLevelMgr.monsterList` 为空，避免最后一波怪还在场上就弹胜利。\n\n课程用 `Invoke` 而不是协程。跟练时我踩过的坑：场景卸载后 Invoke 仍可能触发，所以讲义在回主菜单时会 `Clearinfo` 并销毁实体。自己加功能时要注意 `OnDestroy` 里 `CancelInvoke`。\n\n## 二、MonsterObject\n\n怪物 `Awake` 取 `NavMeshAgent` 与 `Animator`。`InitInfo` 按表设置血量、移速、旋转速度、动画控制器路径。\n\n出生动画结束后 `BornOver()`：\n\n```csharp\nagent.SetDestination(MainTower.Instance.transform.position);\nanimator.SetBool("Run", true);\n```\n\n`Update` 里根据 `agent.velocity` 切换 Run。靠近主塔，距离小于 5，且冷却到了就 `SetTrigger("Atk")`。真正扣血在动画事件 **`AtkEvent`** 里：\n\n```csharp\nVector3 checkPos = transform.position + transform.forward + Vector3.up;\nCollider[] hits = Physics.OverlapSphere(checkPos, radius, mainTowerMask);\nif (hits.Length > 0) MainTower.Instance.Wound(monsterInfo.atk);\n```\n\n动画事件把表现帧和逻辑帧拆开。我第一次漏绑事件时，怪物挥爪主塔不掉血，Console 也没有任何报错，只能靠讲义对照 Animation 窗口里的 Event 列表。\n\n死亡流程：`Wound` 扣血，若 `hp<=0` 则 `Dead()` 关 Agent、播死亡动画、`AddMoney(150)`，再 `Invoke(DeadEvent, 2f)` 从列表移除并 `Destroy`。延迟是为了死亡动画播完，和玩家攻击的即时销毁略有不同。\n\n## 三、PlayerObject\n\n主角在 `Update` 里：\n\n- `Input` 轴驱动 Animator 的 `VSpeed` 与 `HSpeed`\n- 鼠标 X 转 yaw，`roundSpeed` 较高，跟练时讲义强调手感：Shift 切瞄准层权重，R 翻滚，鼠标左键 `Fire` 触发\n\n伤害不在 `Update`，而在动画事件：\n\n| 事件 | 检测 | 伤害 |\n|------|------|------|\n| `KnifeEvent` | `OverlapSphere`，`Monster` 层，半径约 2 | `atk` |\n| `ShootEvent` | `RaycastAll` 从 `gunPoint`，排除 `MainUI` 与自身层 | `atk` 与命中特效 |\n\n跟练时我本地加的调试日志帮助确认：Layer 没建、`gunPoint` 没拖、Monster 层没赋给预制体，都会表现为动画播了、伤害为 0。远程还用 `Debug.DrawRay` 在 Scene 视图看射线方向。\n\n金币：`AddMoney` 更新 `GamePanel`；造塔时 `TowerPoint` 里 `AddMoney(-info.money)` 扣费。\n\n## 四、TowerObject\n\n塔数据 `TowerInfo` 含 `type`：\n\n- **type == 1** 单体：`FindMonster` 取范围内最近存活怪。炮塔 `head` 用 `Quaternion.Slerp` 转向，夹角小于 5° 且 `offsetTime` 冷却到则 `Wound` 加枪口特效\n- **type != 1** 范围：`FindMonsters` 列表，冷却到则中心点特效，列表内全部 `Wound`\n\n```csharp\nvoid FindMonster() {\n    float minDist = float.MaxValue;\n    foreach (var m in GameLevelMgr.Instance.monsterList) {\n        float d = Vector3.Distance(transform.position, m.transform.position);\n        if (d < atkRange && d < minDist) { minDist = d; target = m; }\n    }\n}\n```\n\n`TowerPoint` 负责造塔与换塔。玩家走进触发器时 `GamePanel` 显示可造列表。`CreateTower(id)` 扣钱、`Instantiate`、调 `TowerObject.InitInfo`。若 `nowTowerInfo.next != 0` 表示可升级，UI 显示下一级按钮。\n\n课程 Demo 里 `TowerObject.Start` 有一行测试用 `InitInfo(towerInfoList[6])`。跟练时应以 `TowerPoint.CreateTower` 传入的 `info` 为准，避免场景里预置塔和表数据不一致。\n\n## 五、MainTower 与胜负\n\n`MainTower` 单例在 `Awake` 注册。`Wound` 扣血并刷新 UI，`hp <= 0` 时 `ShowPanel<GameOverPanel>`，`InitInfo(money * 0.5f, false)` 表示失败。\n\n胜利条件在 `MonsterObject.DeadEvent`：`CheckOver()` 为真，所有刷怪点结束且 `monsterList` 空时调用 `GameOverPanel.InitInfo(money, true)`。\n\n两档结算，全额金币 vs 半价，是讲义里的小设计，让玩家感到守塔失败仍有抢救资源的反馈。\n\n## 六、相机\n\n`CameraMove` 在 `LateUpdate` 里根据主角 yaw 算后方偏移加高度，再 `Lerp` 位置、`Slerp` 俯视角度。必须用 **LateUpdate**，否则角色本帧旋转和相机本帧跟随会错一帧，跟拍会抖。Begin 场景的镜头摆动则在 `CameraAnimator` 里用回调衔接 UI，和战斗跟随脚本职责分离。\n\n## 七、复现后记\n\n诚实列一下课程框架之外我本地动过的刀，多为排错，不是系统设计：\n\n- 怪物、主塔、玩家攻击的 Layer 与 `OverlapSphere` 和 `Raycast` 掩码：动画事件是否绑到 `KnifeEvent`、`ShootEvent` 与 `AtkEvent`\n- 音效 `Resources` 路径缺失时的 try-catch 与 Log：退出按钮在 Editor 与打包后分别 `isPlaying=false` 与 `Application.Quit`\n\n没有改表结构、没有新关卡、没有原创塔种。价值在于第一次走完 Unity 从 UI 到物理检测的完整小循环。以后看别的 Unity 项目，能先找单例数据、局内管理、面板字典三条线。\n';export{n as default};
