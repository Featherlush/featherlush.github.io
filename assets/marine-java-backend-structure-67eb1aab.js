const n='---\ntitle: "OceanIndustryStatis 后端结构梳理"\nexcerpt: "Spring Boot 分层、各 Controller 职责，以及图表页联调用的 Report 接口，前端视角的对接笔记。"\ncategory: "后端开发"\ncategoryId: "backend"\ndate: "2024-12-18"\nauthor: "徐宁"\nproject: marine-industry\nseries: spring-boot-notes\n---\n\n智能统计平台的 **Java 后端**非本人开发。我负责图表前端联调。这篇从 **读代码与对接口** 的角度梳理结构，只谈技术分层与数据形状。前端图表页见 [echarts.vue 串联篇](/blog/marine-statistics-echarts-dashboard)。\n\n## 工程概览\n\n- **框架**，Spring Boot，内置 Spring Security\n- **持久层**，MyBatis，mapper 目录下的 xml 映射文件\n- **数据库**，MySQL\n- **模块包**，`oceanindustrystatis` 的 controller、service、mapper、pojo 分层\n\n前端开发时通过 Vite 开发代理访问 Spring Boot 后端；接口路径在 `path.js` 中集中维护。\n\n## 分层结构\n\n| 层级 | 职责 |\n|------|------|\n| `controller/` | REST 入口，参数校验、权限注解 |\n| `service/` | 业务接口 |\n| `service/impl/` | 报表生成、图表聚合等实现 |\n| `mapper/` | MyBatis 接口 |\n| `pojo/` | 实体与 VO，含 `pojo/echarts` 图表专用结构 |\n| `filter/` | 安全、验证码过滤器 |\n| `util/` | RespBean 统一响应封装等 |\n\n典型调用链：\n\n`ReportController` 到 `ReportService` 到 `ReportServiceImpl` 到 `IndustryDetailsMapper` 与其他 Mapper 到 MySQL\n\n图表相关 VO 集中在 `pojo.echarts`：\n\n| 类 | 用途 |\n|----|------|\n| `SunburstVO` | 层级饼图数据，`name`、`value`、`children` |\n| `Waterfall` | 瀑布图 `value`、`difference`、`start` |\n| `StackedLine` | 堆叠折线 `value`、`industry_id`、`industry_name` |\n| `ThermalMap` | 地图 `{ name, value }` |\n\n## Controller 一览\n\n| Controller | 路径前缀 | 主要职责 |\n|------------|----------|----------|\n| `ReportController` | `/report` | **报表生成 与 图表数据**联调重点 |\n| `GetController` | `/get` | 年份列表、城市、GDP、日志等查询 |\n| `SetController` | `/set` | 修改产业值、增速、备注等 |\n| `InitController` | `/init` | 按年季初始化 GDP 比例、产业项 |\n| `CheckController` | `/check` | 核算规则校验 |\n| `ImportController` | `/Import*` | Excel 等数据导入 |\n| `UserController` | `/user` | 用户增删改、登录相关 |\n| `StructureController` | `/structure` | 产业树、类目结构 |\n| `ClassificationController` | `/classification` | 分级策略 |\n| `ActivationCodeController` | `/activation` | 激活码 |\n| `CaptchaController` 与 `EncodeController` | 验证码与加密 | |\n\n前端 `Main.vue` 子路由与这些模块对应：表格审核、文件上传、**可视化 `echarts.vue` 对接 Report**。\n\n## 图表相关接口 ReportController\n\n均为 **GET**，通过 `RespBean.respResult` 输出 JSON。Controller 层典型写法如下：\n\n```java\n@GetMapping("/SunburstByRegion")\npublic void sunburstByRegion(\n    @RequestParam int year,\n    @RequestParam int quarter,\n    @RequestParam String region,\n    HttpServletResponse response) throws IOException {\n  List<SunburstVO> data = reportService.sunburstByRegionName(year, quarter, region);\n  RespBean.respResult(response, data);\n}\n```\n\n| 映射 | 参数主要 | 返回 |\n|------|----------------|------|\n| `/report/SunburstByRegion` | `year`, `quarter`, `region` | `List<SunburstVO>` |\n| `/report/SunburstThreeIndustry` | 同上 | 三次产业 `List<SunburstVO>` |\n| `/report/waterfall-data` | `year`, `industry_id`, `region` | `List<Waterfall>` |\n| `/report/stackedline` | `year`, `industry_id`, `region` | `List<StackedLine>` |\n| `/report/thermalmap` | `year`, `quarter`, `industryName` | `List<ThermalMap>` |\n\n`ReportServiceImpl` 里：\n\n- `sunburstByRegionName` **递归**拼 `children` 树\n- `getWaterfallDataWithDifferences` 计算瀑布 **增量** 与 `start`\n- `getThermalMapData` 按地市列表填充 `ThermalMap`，缺项补 0\n- `getStackedLineData` 按季度拉指定 `industry_id` 序列\n\n部分报表接口带 **角色权限注解** `@Secured`；图表页联调需先完成登录，否则前端会跳回登录页。\n\n## 前端 path.js 与后端的对应\n\n```javascript\nreport: {\n  SunburstByRegion: \'/report/SunburstByRegion\',\n  SunburstThreeIndustry: \'/report/SunburstThreeIndustry\',\n},\necharts: {\n  water: \'/report/waterfall-data\',\n  stackedLine: \'/report/stackedline\',\n  thermalMap: \'/report/thermalmap\',\n}\n```\n\n改后端路径时 **必须同步** `path.js`。\n\n## Mapper 与数据表概念层\n\n联调时记住几类数据源即可：\n\n- **产业明细**，`IndustryDetailsMapper`，瀑布、折线、聚合查询\n- **产业结构**，`IndustryMapper`、`StructureMapper`，产业树\n- **GDP 或 增速**，`GDPMapper`、`SpeedMapper`\n- **用户与权限**，`UserMapper`、`RoleMapper`、`TokenMapper`\n- **日志**，`LogMapper`\n- **专项类目**，`MarineFisheryMapper`\n\n具体 SQL 在 `resources/mapper/` XML 中；改图表字段需前后端一起改 VO 定义。\n\n## 前端联调习惯\n\n1. Network 面板核对 `year`、`quarter`、`region`、`industryName`\n2. `status !== 200` 时先看 `RespBean` 的 message\n3. 地图空白先查 `thermalmap` 数组是否为空，再查 ECharts 注册\n4. 后端逻辑不自行修改时，用 **query 与 期望 JSON 样例** 沟通\n\n## 小结\n\n典型 **Spring Boot 与 MyBatis** 分层后端；图表页只消费 `ReportService` 上的一簇 GET。理清 Controller 到 Service 到 Mapper 链路，前端对接会快很多。\n\n标签：Spring Boot\n';export{n as default};
