const n="---\ntitle: api.js 与 axios 请求层\nexcerpt: 七十多个导出函数收在一个 api.js 里，baseURL、拦截器、REST 模块拆分，以及 GET 封装混用 axios 实例时联调踩过的坑。\ncategory: 前端开发\ncategoryId: frontend\ndate: 2024-07-20\nauthor: 徐宁\nproject: jiuli-app\nseries: vue-notes\n---\n\n前后端分离后，前端所有业务都收敛到 `frame/src/api/api.js` 一个文件，七十多行导出函数。这是我第一次系统性地管理 API。这篇按模块说明设计方式，以及回头看应改进的点。\n\n## 两层 axios：实例与裸用\n\n文件开头创建带拦截器的实例：\n\n```javascript\nconst API_BASE_URL = ({}).VITE_API_BASE ?? '/api'\n\nconst api = axios.create({\n  baseURL: API_BASE_URL,\n  headers: { 'Content-Type': 'application/json' },\n})\n\napi.interceptors.request.use((config) => {\n  const token = localStorage.getItem('Authorization')\n  if (token) {\n    config.headers.Authorization = `Bearer ${token}`\n  }\n  return config\n})\n```\n\n同时，大量 `export async function article()` 里直接用 `axios.get(`${API_BASE_URL}/article`)`，绕开了实例与拦截器。\n\n这导致：\n\n- 部分请求不会自动带 Bearer 若只存了 `token` 键：改环境地址要搜两处 `API_BASE_URL` 与硬编码字符串：竞赛期能跑，维护成本高\n\n改进方向：所有请求统一 `api.get` 或 `api.post`，环境变量只配一处；登录成功后同时写入拦截器读的键名。\n\n## REST 模块与后端路由对齐\n\n前端函数名与 Express `routes/index.js` 挂载路径一一对应，便于联调：\n\n| 前端封装 | 后端路径 | 业务 |\n|----------|----------|------|\n| `loginUser` | POST `/login` | 密码登录 |\n| `registerUser` | POST `/register` | 注册 |\n| `postIdentity` | POST `/choose` | 身份年级 |\n| `article` / `expand` | GET `/article`、`/expand` | 内容列表 |\n| `post` / `comments` / `group` | GET `/post`、`/comment`、`/group` | 社区 |\n| `addPost` / `addComment` | POST 同名 | 发帖评论 |\n| `sendWrongAnswer` | POST `/wrong` | 错题 |\n| `sendRank` 与 `fetchRankings` | POST 或 GET `/ranking` | 排行 |\n| `FetchQuestions` | GET `/wrong/:token` | 个人错题 |\n| `GetID` | POST `/user/id` | token 解 userId |\n\n读列表加前端过滤是常见模式：`getArticleById` 先 GET 全表再 `find(id)`。数据量小竞赛题库时可行；生产应改为 `GET /article/:id`。\n\n## 需要登录的 GET：token 与二次过滤\n\n`allgroup`、`postlike`、`postclick` 等函数流程类似：\n\n1. GET 全量数据\n2. 从 `localStorage` 取 `token`\n3. `GetID({ token })` 换 `userId`\n4. `filter` 出当前用户记录\n\n鉴权与筛选放在前端，后端仍返回全表，竞赛数据量下能跑，但有泄露风险。正确做法：后端按 JWT 只返回当前用户数据，错题路由 `GET /wrong/:token` 更接近这个思路。\n\n## POST 类：统一走 api 实例\n\n`addPost`、`JoinGroup`、`AddPoint` 等用 `api.post(...)`，会自动带 `Authorization` 头若 localStorage 有对应键。body 里往往还要再传 `token` 字段，因为后端 `tokenValidator` 从 body.token 解用户，与 Header 双轨并存，联调时要两边字段都对上。\n\n## 搜索与客户端模糊查询\n\n`article(query)`、`expand(query)` 在拿到全量数组后：\n\n```javascript\narticles.filter(a =>\n  a.title.toLowerCase().includes(query.toLowerCase())\n)\n```\n\n与首页搜索框、分类搜索共用思路。好处是不必等后端搜索接口；坏处是列表变大后性能差。项目里另有 `/history/article`、`/history/group` 记录搜索历史，由后端持久化。\n\n## 题库与排行：业务参数\n\n- `fetchQuestionsForThirdGrade(choose)`：按 `source === '三年级'` 与 `name === choose` 过滤题库\n- `fetchRankings(sortType)`：按 `rightRate` 或 `answerCount` 客户端 sort\n\n接口返回扁平题目或排行行，排序逻辑在前端，便于 UI 切换正确率与答题数 Tab 时即时响应。\n\n## 环境配置建议\n\n- 开发环境 VITE_API_BASE 指向本地 Express 或团队内网网关\n- 生产环境 Nginx 反代同源，前端 baseURL 用相对路径\n- APK 打包前确认 dist 内环境变量与演示服务器策略一致\n\n## 小结\n\n`api.js` 把登录、内容、社区、学习、积分五条业务线收成一张接口地图。核心收获是 axios 实例、拦截器、统一 baseURL 比散落的 `axios.get` 更重要。回头改的话，我会先把裸用 axios 的地方全部收进实例。\n\n标签：Vue.js, Axios, REST API\n";export{n as default};
